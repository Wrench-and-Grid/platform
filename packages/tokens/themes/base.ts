import { colors } from '../colors.ts'
import { fontFamily, fontSize, fontWeight, lineHeight, letterSpacing } from '../typography.ts'
import { spacing } from '../spacing.ts'
import { radius } from '../radius.ts'
import { shadows } from '../shadows.ts'

export const theme = {
  colors,
  fontFamily,
  fontSize,
  fontWeight,
  lineHeight,
  letterSpacing,
  spacing,
  radius,
  shadows,
} as const
